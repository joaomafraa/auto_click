# Autocliquer

App simples em Python para abrir um navegador controlado, gravar cliques e repetir a sequencia ate pressionar `F8`.

## Como rodar

```powershell
pip install -r requirements.txt
python -m playwright install chromium
python autocliquer.py
```
